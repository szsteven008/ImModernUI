#pragma once

#include <string>

namespace ImModernUI {
    bool Separator(
        const std::string& orientation = "horizontal", 
        float size = .0f
    );

    void ShowSeparatorExamples();
}